/*
#define _USE_MATH_DEFINES

#include <iostream>
#include <cmath>
using namespace std;
int main() {
	double p, r;
	cout << "p ? ";
	cin >> p;
	cout << "r ? ";
	cin >> r;
	cout << "M = " << (double) 4 / 3 * (p * M_PI * pow(r, 3.0));
	return 0;
}
*/