/*
#include <iostream>
using namespace std;
int main() {
	double a, b, c, d, e, f;
	cout << "introduce a,b,c,d,e and f:";
	cin >> a >> b >> c >> d >> e >> f;
	if ((c * e - b * f) == 0 || (a * f - c * d) == 0 || cin.fail()) {
		cout << "inconsistent system";
	}
	else {
		cout << "X = " << ((c * e - b * f) / (a * e - b * d)) << endl;
		cout << "Y = " << ((a * f - c * d) / (a * e - b * d));
	}

	return 0;
}
*/